# M37: The non-Latin-1 guards report the cause they hit

**Status:** done (2026-08-25, PR #37 https://github.com/jmgirard/quarto-index/pull/37)

**Goal:** The font guard, the producer reading and the README recipe-block check in `tests/run-tests.sh` each name the cause they actually hit, and the clauses they cannot reach are gone.

**Outcome:** `require_recipe_fonts` surfaces `recipe_font_files`' three own exits (no `mainfont:`, no `mainfontoptions:` block, no `*Font=` face) and reserves the `stix2-otf` sentence for the `kpsewhich` miss, which also names the mis-assembled-face reading it cannot tell apart. `pdf_producer_names` reads `pdfinfo` into a variable so its three `return`s are its only statuses; a backgrounded-subshell self-test call under errexit reaches it unshielded. The unreachable `[ -n "$faces" ]` and `if not stated:` clauses are gone. Three named builders — `m37_drop_options_block`, `m37_add_unstated_line`, `m37_strip_error_reports` — replace the M35 mutations, each bounded to the region its label names, each exiting non-zero over an input carrying no such region, each shown byte-identical outside its bound on a fixture where the M35 form (kept beside it as `*_m35_form`) diverged. A green run prints the guard's face list. 354 plain / 496 self-test, from 352 / 491.

**Decisions:** four milestone-local, all narrow-not-widen — F3 mis-parses surface as the face the guard could not assemble rather than widening the YAML parser; F6 the face list prints; F8 the recipe-block fixture direction stays a whole-file substring search and says so; F12 `M33_NOENGINE_PRODUCER` and README's `lualatex` stay two hand statements no check compares. The Scope prose's `D-118` citation names no decision in this repo and is not carried forward.

**Review:** three-lens fan-out; thirteen findings. Five fixed on the branch — a `kpsewhich` message asserting a package as missing for a cause that package would not fix, a status assertion whose failure text over-claimed, and three comments that miscounted returns and call sites or rested on bash 3.2's empty-array behaviour. Four absorbed into the acceptance-suite hardening candidate row: a bound assertion recomputing its bound with the builder's own regex, `^#{2,3} ` matching one character into a `#### ` heading, mutation anchors matched anywhere in the file, a hand-written loud-failure fixture. Four rejected. No return-floor trigger. Nothing graduated or retired.
