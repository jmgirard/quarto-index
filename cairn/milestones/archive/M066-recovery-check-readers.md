# M066: The recovery-route checks read the reports and mutations they name

**Status:** done (2026-09-01, PR #66 https://github.com/jmgirard/quarto-index/pull/66)

**Goal:** The checks fencing M064/M065's source-recovery route read every record wording their own labels claim, prove each substitution they splice, and hold the recovery prose the books page states.

**Outcome:** Three read-repairs in `tests/run-tests.sh`, each leaving what its check promises unchanged (D-011). Every `check_warning_count` site asserting a literal 0 of one of the six `WARN_STORE_*` record-report wordings now sits beside a literal-count site for the other two wordings of its family, on the same log in the same function — 31 sites added across 27 short groups (the three sites the plan named as models were among the short ones), each label extending its neighbour's tail to name the wording added; the enumeration reads 87 literal-0 sites and 0 short. `m061_mutant` takes `<slug> <label> <substitution>...`, one perl process applying each argument in turn and writing its match count to `$M061W/<slug>-counts`; a count of 0 fails naming the substitution's ordinal and its first 72 characters, an ordinal outside the arguments or a short count file fails, and the whole-file `cmp` stays as a backstop. The `m065-carryrange` site's two substitutions are `M065_CARRYRANGE_CARRY` and `M065_CARRYRANGE_PAIR`, and `m066_slipped` under `--self-test` plants each slipped alone (a `;` for the `,` the pattern anchors on, `{slipped}` for a replacement), holding the exit non-zero, the message to name that ordinal, and the spliced file to still differ — the half-applied case a whole-file compare passes. Twelve claim rows hold `site/books.qmd:84-135` (the recovery route, the no-marks report, the five-things lead, the `entry=`/`sort=`/`see=` sentence, the include/cell bullet, no fragment, conditional content out whole, the unreadable-source bullet, no range and no principal, an absent record, an unlistable store directory, a store that still lists), 21 rows in all, and the count-coupled self-test string reads 21.

**Decisions:** none cross-cutting. Milestone-local: AC1 amended at the mini gate to "a `check_warning_count` site for each of the other two wordings, each asserting a literal count", because the model sites hold one wording at 1 where the log draws it.

**Review:** Full three-lens fan-out (executable surface). All four criteria green on fresh evidence — 578 checks plain, 1071 with `--self-test`, both exit 0, re-run over the gate fixes — and the consistency gate clean. The blame-history and prior-review lenses found nothing (the PR-comment probe returns `[]`); the diff-bug lens returned fifteen findings, none meeting the return floor. Seven fixed at the gate: three claim rows added and the topic-sentence row replaced, the counts loop bounds the ordinal before expanding it, the backstop names the counts file, the perl death message no longer says "did not compile", and the slipped expressions carry no live replacement text. One refuted against the implementation (a `;`-joined argument fails loudly); one plan-prose inaccuracy corrected in the Review section; five rejected (promise unchanged under D-011, two style, plan-owned line numbers, a tick the fresh runs settled). KI222 records the remainder — over-application is counted and never checked — routed to the M37 guards row at a disposition gate. No lesson added or retired: LESSONS stands at 49/50 lines and 19,912/20,000 bytes.
