# M39: The sort-key rival and dangling-target reports name the index they judge

**Status:** done (2026-08-26, PR #39 https://github.com/jmgirard/quarto-index/pull/39)

**Goal:** In a document that declares several indexes, the sort-key rival report says which index the rivalry is inside, and the dangling-target report's remedy names the index the term has to be marked in.

**Outcome:** `sortkeys.register_sort` and `marks.report_dangling` each gained a SECOND `warn()` literal, drawn only where `qi_indexes.scope_phrase` hands back something other than the caller's outer word — a declaring document under an unfolded back-end. The rival shape carries `in index "<name>"` between the winning key and the tail both shapes share; the dangling shape names the index as the set judged against and again as the place to mark the term, replacing the shared `mark that term somewhere`. Both take the scope word as `(outer, index)` and call `scope_phrase` themselves; `index.lua`'s per-index call and `book.lua`'s aggregate call thread it. Scope clauses are mid-message, not appended, so neither template is a prefix of another. New fixture `examples/named-indexes-rival.qmd` (two indexes, the rivalry in the second); suite gains `WARN_DANGLING_INDEX`, `WARN_SORT_RIVAL_SCOPED`, `dangling_report_index()` and an M39 render block, and `warn-distinct` EXPECTED goes 64 → 66. Every pre-existing message literal survives byte for byte.

**Decisions:** D-022 (annotates D-021) — the second cost D-021 accepted is paid, its scope-word rule unchanged, and its deferral pointer corrected from the suite-hardening row to the report-scope row M39 absorbed.

**Review:** Three-lens fan-out (executable surface). All five criteria met with fresh evidence: three renders, `warn-distinct` at 66 distinct prefix-free templates, `--self-test` green at 562 checks. Six findings. Fixed on the branch: `DESIGN.md`'s "modules, in dependency order" list still put `sortkeys.lua` above `indexes.lua`, which this diff made it require; and a 119-character unwrapped line in Conventions. Raised at the gate and kept as written: the new dangling remedy's `which no mark of index "authors" indexes` clause. Rejected: branching on `scope == outer` rather than `is_declared()`/`folds()` (that comparison IS `scope_phrase`'s recorded contract); the stale ROADMAP hygiene stamp (replaced by this pass). Routed to hygiene: a LESSONS line for the `warn(`-in-a-comment scan hazard. No defect return. Hygiene pruned the stalest lesson (M01's compile-don't-review escape-table line) to make room for it.
