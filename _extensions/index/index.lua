-- quarto-index: format-neutral index marks -> per-format back-ends.
-- T1 scaffold: registration only; entry semantics arrive in T2.

return {
  {
    Span = function(span)
      return span
    end,
  },
}
