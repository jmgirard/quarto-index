# M10: Self-references the level fold and empty levels hide

**Status:** done (2026-08-18, PR #10 https://github.com/jmgirard/quarto-index/pull/10)

**Goal:** Drop and report a cross-reference target that names what the mark's own entry prints, in the two cases M08's comparison cannot see: after the LaTeX three-level fold, and where an empty level makes the two spellings differ.

**Outcome:** Two self-target comparisons where M08 had one. The format-neutral one gained `nonempty_levels` stripping on both sides — an empty level prints nothing, so `entry="Cats!"` and a target of `Cats` are one printed path; M08 dropped empties on the target side only, so the asymmetry was in the comparison, not the mark. A second comparison runs in the LaTeX branch after `clamp_levels`, against the clamped printed path, catching a target that only the fold makes self-referential; `index_argument` returns its clamped levels rather than letting the caller recompute them, since `clamp_levels` warns and a second call would report the fold twice. The fold rule stays inside the back-end whose fold it is: HTML has no ceiling and keeps such targets. Five fixture shapes in examples/self-xref.qmd (depth-4 `see-also`, depth-5 `see`, a folded entry carrying `sort=` where filing and printed paths differ, and two empty-level shapes). Suite 150 to 169 checks, evidence running to the compiled PDF per GP6. DESIGN.md's Span-pass and LaTeX back-end prose corrected.

**Decisions:** none cross-cutting. The plan gate chose the hybrid comparison over moving it wholly into the back-ends (a back-end-local comparison stops diagnosing self-targets in gfm, which DESIGN commits to), and chose dropping a fold-induced target over reporting and keeping it.

**Review:** Three-lens fan-out; blame-history and prior-review-record returned zero findings. The [O] diff-bug lens verified both comparisons against live renders and returned ten. Five fixed on the branch: DESIGN.md still described the fixed defect; the grep-key distinctness check read only each `warn()` call's leading literal; the PDF check omitted AC2's IP2 half; a duplicated `levels_key` derivation; a bare `see` substring scan. Four to candidate rows (the leading-empty behaviour change and the trailing-vs-leading null-field asymmetry widened the empty-levels row; the doubled warning on an empty-only fold and the never-reported unresolvable target got rows). One rejected as latent: the 4th return value can alias the caller's table, but nothing mutates it. Zero defect returns; one amendment return — AC7 promised the verify slot passes "on a clean checkout", which is false and predates M10 (a fresh clone of `main` fails identically at `examples/control.tex`), and is verbatim an acceptance-suite-hardening item Scope puts Out.
