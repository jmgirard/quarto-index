---
pagetitle: "quarto-index"
---

# quarto-index

A Quarto extension for book-quality subject indexing. Mark index entries with
a format-neutral span syntax; the extension emits the right thing per output
format. Two back-ends ship: LaTeX/PDF and HTML.

This site lives at <https://jmgirard.github.io/quarto-index/>. It is built from
`site/` in the [source repository](https://github.com/jmgirard/quarto-index)
and republished by GitHub Actions on every push to the default branch, so the
pages here describe the extension as that branch has it.

## Install

```bash
quarto add jmgirard/quarto-index
```

Then enable the filter in your document (or `_quarto.yml`):

```yaml
---
title: "My book"
filters:
  - index
---
```

Requires Quarto 1.4 or later. No other runtime dependencies: on the LaTeX
side it uses `imakeidx`, which ships with mainstream TeX distributions.

## Editor support

The extension ships two files an editor reads. `_schema.yml` is its Quarto
Wizard schema: it declares the two classes the filter reads, `index` and
`qi-index-here`, and every attribute each one carries, with a description
apiece. `_snippets.json` is VS Code-format snippets, one per marking form,
with example terms to type over. Editors supporting those formats — Quarto
Wizard in VS Code and Positron — read them for completion and hover text, and
insert a snippet on its own prefix. Nothing in a render reads either file, so
an editor that does not know them changes nothing about what you get.

## Where to go next

- [Syntax](syntax.qmd) — the supported marking forms, and a page per topic:
  [sub-entry levels](sub-entry-levels.qmd),
  [cross-references](cross-references.qmd),
  [special characters](special-characters.qmd),
  [terms outside Latin-1](terms-outside-latin-1.qmd),
  [sort keys](sorting.qmd),
  [principal mentions](principal-mention.qmd),
  [page ranges](page-ranges.qmd),
  [placing the index](placing-the-index.qmd) and
  [named indexes](named-indexes.qmd).
- [What it emits](output.qmd) — what each output format gets, and where the
  two back-ends differ.
- [Books](books.qmd) — one index for a whole Quarto book.
- [Examples](examples.qmd) and [Tests](tests.qmd) — the fixture corpus and the
  acceptance suite.
