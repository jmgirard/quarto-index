# Design

## Purpose & Scope

<!-- Seeded by cairn-init 2026-08-16 in a greenfield repo; refine as the
     project takes shape (`/design-interview` elicits the deep version). -->

A greenfield project named `quarto-index`; presumed Quarto-related (extension,
website, or indexing tooling), but the concrete deliverable is not yet
declared — the first milestone plan should state it here. Distribution
ambition (declared at init): **tagged public releases** — versioned git tags
and GitHub releases, with changelog discipline from the start. Toolchain
profile: generic (see `cairn/PROFILE.md`).

## Function Families

_None yet — populated as the codebase takes shape._

## Conventions

- Numeric results carry **no oracle-verification commitment** (declared at
  init, 2026-08-16): the universal ≥2-independent-oracle-types bar is waived;
  numeric results, if any arise, are checked ad hoc. Revisit if scoring or
  statistical work enters the project.

## Design Principles

<!-- IP<n> = Inviolable (hard constraint) block first, then GP<n> = Guiding
     (tradeable with justification); numbers never reused. Principles are
     elicited from the user (`/design-interview`), never invented. -->

_None yet._

## Architecture

_None yet — describes the system as it **is**, once it exists._

## Known issues

_None._
