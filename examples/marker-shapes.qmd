---
title: "quarto-index marker-shape probe with [x]{.qi-index-here} in the title"
from: markdown-smart
filters:
  - index
# MANIFEST — the emptied-place report expected from this fixture.
#
# Derivation: one line per marker whose removal leaves nothing the author
# wrote in the block list it stood in, numbered by the 1-based index of the
# TOP-LEVEL block it is written under, counted down the body below (the H1s
# and the explanatory paragraphs are top-level blocks and are counted). The
# numbers come from this file's structure, never from a render.
#
# That works HERE only because this file holds no include and no executable
# cell, so the blocks Quarto hands the filter are the blocks written below,
# one for one. The report's number is counted over what the filter is handed,
# after Quarto's own processing — not over the author's source file — and
# examples/marker-position.qmd is the fixture where the two diverge. Add an
# include or a cell above and every number here would move.
#
# A shape is
# listed here BECAUSE the marker is the only thing in its list, so the list is
# what the manifest is about; the container kind is deliberately absent,
# because the report never names one.
#
#   reports at top-level blocks: 12 13 14 15 16 17 18 20 22 24 26 26 28
#
#     12  a Quarto callout holding only the marker
#     13  a tabset whose first tab holds only the marker
#     14  a captioned figure whose body is only the marker
#     15  a block quote holding only the marker
#     16  a bullet list whose only item holds only the marker
#     17  a grid-table cell holding only the marker
#     18  a footnote body holding only the marker
#     20  a marker alone inside a marker — ONE report, not one per level
#     22  a marker three deep — ONE report
#     24  a marker inside a marker inside a FOOTNOTE — still ONE report
#     26  a bullet list with TWO marker-only items (first of the two)
#     26  a bullet list with TWO marker-only items (second of the two)
#     28  a definition list whose first definition holds only the marker
#
#   No report anywhere else. The must-not-report shapes are written without a
#   bare leading number, so the reporting rows above are the only ones a parse
#   of this manifest picks up:
#     block 5 — a marker carrying content, which is spliced back in
#     block 7 — a marker beside a paragraph the author wrote
#     block 9 — a container the author left empty, holding no marker at all
#     block 31 — the top-level placement marker, removed whole, not emptied
#     the title — the marker class in metadata, which the machinery never reaches
---

# Shapes that must NOT be reported

The document title carries the marker class. Metadata is not a placement site
at all — the marker machinery never reaches it — so nothing about it is
reported. A report that cannot tell metadata from the document body tells an
author something untrue, which is worse than saying nothing.

One term is marked here: [theta]{.index}.

A marker below the top level places nothing, and what it carried is spliced in
where it stood — so the container it was written in keeps that content.

::: {#keeps-content .surrounding}
::: {.qi-index-here}
Content the marker kept, which leaves this container non-empty.
:::
:::

A marker written beside something the author wrote empties nothing either, so
the report must stay silent here however the marker itself is handled.

::: {#keeps-sibling .surrounding}
A paragraph the author wrote, which survives the marker beside it.

::: {.qi-index-here}
:::
:::

A container the author left empty was never emptied by this extension, so it
is none of its business and draws no report.

::: {#author-empty .surrounding}
:::

# Shapes that must be reported

Each container below holds nothing but a marker, so removing the marker leaves
nothing the author wrote where it stood.

::: {.callout-note}
::: {.qi-index-here}
:::
:::

::: {.panel-tabset}
## Tab holding only a marker

::: {.qi-index-here}
:::

## Tab holding text

Text, so the tabset itself keeps something.
:::

::: {#fig-marker}
::: {.qi-index-here}
:::

A caption, which is not the figure's body.
:::

> ::: {.qi-index-here}
> :::

- ::: {.qi-index-here}
  :::

+----------------------+------------------------+
| ::: {.qi-index-here} | A cell the author       |
| :::                  | wrote text in.          |
+----------------------+------------------------+

: A table whose first cell holds only a marker.

A footnote whose body holds only a marker.[^only]

[^only]:
    ::: {.qi-index-here}
    :::

A marker alone inside a marker empties one place, not two: the inner marker is
removed whole, so it is nothing an author could find left empty.

::: {#doubly .surrounding}
::: {.qi-index-here}
::: {.qi-index-here}
:::
:::
:::

Three deep is still one place.

::: {#triply .surrounding}
::: {.qi-index-here}
::: {.qi-index-here}
::: {.qi-index-here}
:::
:::
:::
:::

Nesting inside a footnote is the same one place, and is its own shape because
a footnote's blocks are reached differently from every other container's.

A footnote holding a marker inside a marker.[^nested]

[^nested]:
    ::: {.qi-index-here}
    ::: {.qi-index-here}
    :::
    :::

Two places emptied under ONE top-level block draw two reports, and the two are
byte-identical because the report carries the block's position and nothing
finer. That is the honest limit of naming nothing, so it is pinned rather than
avoided.

- ::: {.qi-index-here}
  :::

- ::: {.qi-index-here}
  :::

A definition list is the container M08's kind-by-kind check needed its own
branch for. Here it is a block list like every other.

Term whose definition is only a marker

:   ::: {.qi-index-here}
    :::

Term with a real definition

:   Text, so this definition keeps something.

# The shape that places the index

The top-level marker below wraps a nested one. The outer marker is removed
whole — it places the index and reaches no output — so it is not a container
anything was emptied out of.

::: {.qi-index-here}
::: {.qi-index-here}
:::
:::

# After the marker

Text after the marker, so the index's position is provable.
